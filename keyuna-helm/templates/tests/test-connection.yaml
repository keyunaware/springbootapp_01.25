apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keyuna-helm.fullname" . }}-test-connection"
  labels:
    {{- include "keyuna-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keyuna-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
