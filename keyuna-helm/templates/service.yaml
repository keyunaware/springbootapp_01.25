apiVersion: v1
kind: Service
metadata:
  name: {{ include "keyuna-helm.fullname" . }}
  labels:
    {{- include "keyuna-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keyuna-helm.selectorLabels" . | nindent 4 }}
